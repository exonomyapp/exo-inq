# Procedure: Install Unzip

## Objective
Install the `unzip` utility, which is a dependency for installing Bun.

## Procedure
1. Update package list: `sudo apt-get update`
2. Install unzip: `sudo apt-get install -y unzip`

## Verification
- Confirm installation: `unzip -v`
